# Used by the runtime-config endpoint to prefill the auth demo backend URL.
# In Docker, set API_BASE_URL on the running container instead of baking it at build time.
API_BASE_URL=http://127.0.0.1:4000